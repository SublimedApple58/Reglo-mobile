# Guide di gruppo (Group lessons) — mobile

## What it does
Mobile side of the **Guide di gruppo** module (1 istruttore + 1 veicolo + fino a 3 o 4 allievi, 3–4h). Optional module, gated on `settings.groupLessonsEnabled` (backend `CompanyService.limits`). Istruttore/titolare creano e gestiscono le guide di gruppo dall'agenda; l'allievo riceve un invito e si iscrive.

## Accent / design
Card gemella dell'esame ma con **accento teal/emerald** (icona 3D Fluent `fluent-people.png`):
- surface `#ECFDF5`, ombra `#10B981`, label/icona `#0F766E`/`#047857`, indicatore posti (quadratini pieni/vuoti).
- Esame = viola, Guida singola = navy, **Guida di gruppo = teal**. Mono-navy per tutto il resto.
- Preview design approvata: `plans/frontend/010-group-lessons-preview.html` (+ `.png`).

## Key files
- **Tipi** `src/types/regloApi.ts`: `GroupLesson`, `GroupLessonInvite`, `GroupLessonParticipant`, `GroupLessonInvitee`, `CreateGroupLessonInput`; `AutoscuolaAppointment.groupLessonId`; `AutoscuolaStudent.groupLessonsOptIn`; `AutoscuolaSettings.groupLessonsEnabled`.
- **Service** `src/services/regloApi.ts`: `getGroupLessons`, `getGroupLesson` (single, GET `/api/autoscuole/group-lessons/{id}`), `createGroupLesson`, `updateGroupLesson` (PATCH same URL — startsAt/endsAt/instructorId/vehicleId applies to all; notes container-only), `cancelGroupLesson`, `add/removeGroupLessonParticipant`, `inviteToGroupLesson`, `getEligibleGroupLessonInvitees`, `getGroupLessonInvites`, `respondGroupLessonInvite`, `updateStudentGroupLessonOptIn`.
- **Agenda (rendering)**:
  - `src/utils/weeklyAgenda.ts` — `DayGroupLessonGroup` + `groupLessonGroups` (participant appts `type==='group_lesson'` grouped by `groupLessonId`; capacity dalla riga (`groupLessonCapacity`, fallback `GROUP_LESSON_CAPACITY=3`)), `'group'` density segment, count in `daySummary`/`weekTotals`.
  - `src/components/DayItinerary.tsx` — `groupCard` (teal, Fluent people icon, seats indicator) + `onOpenGroupLesson`.
  - `src/components/WeeklyOverview.tsx` — `segGroup` (`#10B981`).
  - `src/stores/dayDetailStore.ts` + `app/(tabs)/home/day-detail.tsx` — `onOpenGroupLesson`.
  - `src/screens/IstruttoreHomeScreen.tsx` — `timelineStatusConfig` teal "GRUPPO" badge for `type==='group_lesson'`; `openGroupLessonManage(groupLessonId)` loads the instructor+vehicle lists then seeds `groupLessonManageStore` and pushes the `manage-group-lesson` page sheet; wired from **both** the day-detail card (`onOpenGroupLesson`) and the hour-grid card (`openLessonDrawer` short-circuits when `type==='group_lesson' && groupLessonId`); `openCreateGroupLesson`; FAB → add-action row "Guida di gruppo" (gated on `settings.groupLessonsEnabled`).
- **Gestione istruttore** (NEW): "Gestisci guida di gruppo" page sheet, design piatto clonato da `manage-lesson` (overline + titolo + meta + state-pill teal · ring centrale teal `posti/capienza` label "POSTI" · righe piatte Istruttore/Veicolo · toolbar flottante).
  - `app/(tabs)/home/manage-group-lesson.tsx` (route `presentation:'modal'` page sheet) — carica il detail via `getGroupLesson`, auto-save su selezione istruttore/veicolo (`updateGroupLesson`, vale per tutti), CTA 3D "Partecipanti" → roster sheet, toolbar **Sposta** (data→ora→durata 1–4h → `updateGroupLesson`) + cestino (`cancelGroupLesson`, conferma `Alert`). Niente "Luogo" (non gestito per i gruppi lato web). **La riga "Note" contenitore è stata rimossa (2026-06-16)**: le note sono ora **per-allievo** nel roster.
  - `app/(tabs)/home/manage-group-lesson-participants.tsx` (route `formSheet` content-hugging, X no-grabber, no ScrollView) — roster: ogni riga allievo è **tappabile per la nota per-allievo** (sotto il nome: anteprima nota o "Aggiungi nota" come cue teal) → `notesEditorStore` → `edit-notes` → `updateAppointmentDetails(appointmentId, {notes})` → ricarica `getGroupLesson`; rimozione con **✕ grigia discreta** (`removeGroupLessonParticipant`); riga "Aggiungi allievo" → picker idonei (`getEligibleGroupLessonInvitees` → `select-options` → `addGroupLessonParticipant`); bottone "Invita allievi idonei · N posti" (`inviteToGroupLesson`). `GroupLessonParticipant.notes` arriva da `getGroupLesson`.
  - Store: `src/stores/groupLessonManageStore.ts` (seed: groupLessonId + instructors + vehicles + vehiclesEnabled + onChanged/onClosed) e `src/stores/groupLessonParticipantsStore.ts` (seed: groupLessonId + lesson + onChanged). Riusa i picker esistenti `manage-lesson-instructor` (istruttore), `select-options` (veicolo/durata/aggiungi-allievo), `select-date` (data), `time-picker` (ora).
- **Creazione** `src/screens/CreateGroupLessonScreen.tsx` + route `app/(tabs)/home/create-group-lesson.tsx` (formSheet) + `src/stores/groupLessonSheetStore.ts`. Reuses `select-date`, `time-picker`, `select-options` (durata/veicolo/istruttore), `select-exam-students` (multi-select allievi). Allievi pre-inseribili = opted-in + license-compatibile col veicolo scelto. Toggle "Apri i posti agli inviti" → `inviteToGroupLesson` dopo la create.
- **Opt-in allievo** `src/screens/StudentNotesDetailScreen.tsx` — toggle "Guide di gruppo" (mostrato se `groupLessonsEnabled`) → `updateStudentGroupLessonOptIn`. Standalone (fuori dalla flip-card per non confliggere col gesto).
- **Invito allievo** `src/screens/GroupLessonInvitesScreen.tsx` + route `app/(tabs)/home/group-lesson-invites.tsx` (solo STUDENT). Lista inviti (da `getGroupLessonInvites`), card stile **Airbnb / mono-navy**: kicker teal "Guida di gruppo · N posti liberi", **data in primo piano** (`Venerdì 12 giugno`) + range orario con durata, istruttore/veicolo come meta-righe leggere (Ionicons outline), **posti come pallini** (teal pieni vs navy-200 vuoti). Rassicurazione testuale "Non scala i tuoi crediti guida" (check teal) — **nessun prezzo mostrato** (price band rimossa, niente `lessonPrice60`). Iscrivimi / Non mi interessa → `respondGroupLessonInvite`.
- **CTA home allievo** `src/screens/AllievoHomeScreen.tsx` — riga-CTA "Guide di gruppo" (gemella di "Scambi", icona Fluent people, badge **teal** = conteggio inviti via `getGroupLessonInvites`), tap → `home/group-lesson-invites`. Gated su `groupLessonsEligible = settings.groupLessonsEnabled && selectedStudent.groupLessonsOptIn` (solo allievi abilitati e opt-in).
- **Guide di gruppo ISCRITTE — card home allievo + ritiro** (NEW, 2026-06-10):
  - `AllievoHomeScreen.tsx` — gli appuntamenti `type==='group_lesson'` sono **esclusi** dall'hero/mini (`upcoming`) e mostrati in una **sezione teal dedicata** (`upcomingGroupLessons`): card full-width modellata sulla card guida-di-gruppo dell'agenda istruttore (`#ECFDF5`, label `#0F766E`, **pallini posti** `#10B981`/vuoti `#BDEAD6`, time-forward). I posti (`filled/capacity`) sono caricati per id via `getGroupLesson` (poche guide → `groupSeats` state); senza dati la card va senza pallini. Tap → `openGroupLessonDetail` semina `groupLessonStudentStore` e pusha `home/group-lesson-detail`.
  - `app/(tabs)/home/group-lesson-detail.tsx` (route **`presentation:'formSheet'` content-hugging** — `fitToContents`, no grabber, no ScrollView, root senza `flex:1`) + `src/stores/groupLessonStudentStore.ts` — **sheet allievo**, stile clonato dal "Gestisci guida" istruttore ma **minimale**: kicker teal "Guida di gruppo" + **orario in risalto** (`heroClock` 30px) + giorno/durata. Posti come **pallini grandi anonimi** (niente nomi degli altri allievi — `getGroupLesson` è student-safe), righe piatte Istruttore/Veicolo sola lettura, bottone "Ritira iscrizione" in fondo al flusso. **Niente** banner crediti, **niente** state-pill posti, **niente** banda arancione (richiesta utente: meno colore). **Skeleton fedele a dissolvenza**: ogni campo dinamico (orario, giorno, pallini posti, nomi istruttore/veicolo) è un `Slot` ad **altezza fissa** che mostra uno `SkeletonBlock` shimmer finché i dati non arrivano, poi fa **cross-fade** (`FadeIn`) del valore — altezza stabile → niente salto del detent, comparsa morbida. CTA "Ritira iscrizione" (destructive) → conferma `Alert` con **avviso penale** se entro il `penaltyCutoffHoursPreset` → `regloApi.withdrawFromGroupLesson(groupLessonId)` → `onChanged` (invalida le query) + `router.back()`.
  - `regloApi.withdrawFromGroupLesson(groupLessonId)` → `POST /api/autoscuole/group-lessons/{id}/withdraw`.
- **Notifiche** `src/types/notifications.ts` (kind `group_lesson_invite`), `src/screens/NotificationInboxScreen.tsx` (icona `people-outline`, theme teal, title/subtitle; tap → `home/group-lesson-invites`). Recovery dal BE `/api/autoscuole/notifications`.

## Pagamento
La guida di gruppo **non scala crediti**: ogni partecipante ha un appointment "da pagare" (`paymentRequired`, `paymentStatus="pending"`, `manualPaymentStatus="unpaid"`) al **prezzo di una guida normale** (`lessonPrice60`, nessun prezzo dedicato). La gestione del pagamento è **solo backend/web** (il titolare addebita/archivia): l'app mobile **non mostra più** pagamenti né importi all'allievo (pagamenti in-app rimossi).

**Ritiro / rimozione (decisione A, 2026-06-10)**: si riusa la logica delle guide normali (lato backend). Ritiro/rimozione **prima del cutoff** = posto liberato senza addebito; **dopo il cutoff o a guida avvenuta** = resta "da pagare" e finisce nelle *cancellazioni tardive* (il titolare addebita/archivia). Il posto liberato fa **re-broadcast** dell'invito; il ritiro dell'allievo notifica l'istruttore. Dettagli backend: `reglo/docs/features/group-lessons.md`.

## Note / limiti noti
- Sia la vista **giornaliera** (hour-grid, `IstruttoreHomeScreen` `timelineItems`/`renderRow` kind `groupLesson`) sia la **settimanale** (day-detail sheet) collassano i partecipanti in **una sola card** per guida (teal, icona 3D Fluent people, **niente nome allievo**, pallini posti `count/capacity`, card più grande di una guida normale). Tap → "Gestisci guida di gruppo" page sheet.
- Le guide di gruppo **vuote** (0 partecipanti) ora compaiono anche in agenda: il bootstrap backend (`getAutoscuolaAgendaBootstrapAction`) sintetizza una riga `gl-empty:<glId>` (fonte unica web+mobile); `weeklyAgenda.ts` e `IstruttoreHomeScreen.timelineItems` escludono le righe `gl-empty:` dal conteggio (mostrano `0/3`). Così sono gestibili/annullabili anche senza iscritti.
- **Capienza configurabile 3 o 4** (2026-06-12): riga "Capienza" nel form di creazione e nella sheet di gestione (auto-save, il BE rifiuta sotto gli iscritti attuali). Le card agenda (giorno/settimana/griglia) leggono la capienza reale dall'annotazione BE `groupLessonCapacity` sulle righe (fallback 3); le UI allievo (inviti, card home, detail sheet) erano già dinamiche su `capacity` dal payload.

## Connected features
- Backend `reglo/docs/features/group-lessons.md`, `vehicles` (patenti → filtro invitabili), `notifications` (kind nuovo), pagamenti.

## Capienza libera + turni moto + colore arancio (2026-07-06)

- **Capienza libera 1–12 per entrambi i tipi** (prima 3/4, e per moto = flotta): picker 1–12 in `CreateGroupLessonScreen` (riga Capienza ora visibile anche per moto) e in `manage-group-lesson.tsx` (opzioni sotto gli iscritti marcate non disponibili); il fleet picker non forza più `capacity: values.length`.
- **Turni moto**: gli allievi possono superare le moto in flotta — eligibilità = solo gerarchia (una moto in flotta che serva il percorso), assegnazione best-effort. Partecipante senza moto → roster "Moto a rotazione" (`manage-group-lesson-participants.tsx`), detail allievo "A rotazione con il gruppo" (`group-lesson-detail.tsx`).
- **Colore arancio guide moto** (stesso stile del teal): bg `#FFF4EA`, ombra/accento `#F97316`, testo `#C2410C`, posto libero `#FCD9B8`. Il BE annota `groupLessonKind` sulle righe agenda (tipo in `types/regloApi.ts`). Superfici: `DayItinerary` (groupCardMoto), `WeeklyAgendaView` (`GROUP_MOTO_LOOK`, label "Gruppo moto"), timeline `IstruttoreHomeScreen` (glKind), sezione guide di gruppo in `AllievoHomeScreen` (kind da `getGroupLesson`).

## Moto group lessons (kind="moto", 2026-06-25)
A second flavour: a **fleet of motos** + **one shared follow car**; each participant rides their **own** moto (mixed A1/A2/A allowed) — the moto is **auto-assigned** by the backend on enrol. (Dal 2026-07-06 l'eligibilità è gerarchia-only e l'assegnazione best-effort — vedi sezione sopra.)
- **Types** (`src/types/regloApi.ts`): `GroupLesson.kind/followVehicleId/followVehicleName/fleet`, `GroupLessonParticipant.vehicleName/licenseCategory`, `CreateGroupLessonInput.kind/vehicleIds/followVehicleId`.
- **Create** (`CreateGroupLessonScreen`): Standard/Moto segmented toggle; moto → multi-picker "Moto della guida" (fleet) + "Auto al seguito" picker **sempre facoltativo (2026-07-06)**: se le regole la richiedono, il BE la assegna automaticamente alla prima iscrizione (placeholder "Automatica alla 1ª iscrizione").
- **Manage (mobile) — moto-aware (2026-06-26):** `manage-group-lesson.tsx` branches on `lesson.kind === 'moto'`: title "**Guida di gruppo moto**"; the single editable "Veicolo" row (which showed "Nessun veicolo" for moto, since the container `vehicleId` is null) is replaced by **"Moto della guida"** (fleet — multi-picker over moto vehicles → `updateGroupLesson({vehicleIds, capacity: n})`, capacity follows fleet size) + **"Auto al seguito"** (single picker over category-B cars, "Nessuna" clears → `updateGroupLesson({followVehicleId})`). The generic 3/4 capacity row is hidden for moto (implicit = fleet size). Reads `lesson.fleet` + `followVehicleName` from `getGroupLesson`. The participants roster (`manage-group-lesson-participants.tsx`) shows under each student the **license they're pursuing** ("Patente A2", from `participants[].licenseCategory` = assigned vehicle category) + the assigned moto for moto groups.
- **Student view — moto-aware (2026-06-26):** the enrolled student never sees a shared lesson vehicle (there is none — it's a fleet). `group-lesson-detail.tsx` now branches on `lesson.kind === 'moto'`: kicker "**Guida di gruppo moto**", vehicle row relabelled "**La tua moto**" showing the self participant's assigned moto (`participants.find(p => !!p.studentId).vehicleName`, fallback "Da assegnare" since it's assigned at enrol), plus an "**Auto al seguito**" row from `followVehicleName`. The invites list (`GroupLessonInvitesScreen`) shows "**Ti verrà assegnata una moto**" (`bicycle-outline`) for `kind === 'moto'` instead of a vehicle name — requires the backend `getGroupLessonInvites` to expose `kind` (added in `reglo/lib/actions/autoscuole-availability.actions.ts`; mirrored on `GroupLessonInvite.kind`). See `student-moto-experience.md`.
