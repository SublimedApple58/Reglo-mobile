import React from 'react';

import { StudentSettingsSheet } from '../../../src/components/StudentSettingsSheet';

/** Form sheet "Impostazioni allievo" — registrato in entrambi gli stack che
 *  aprono la scheda allievo. */
export default function StudentSettingsRoute() {
  return <StudentSettingsSheet />;
}
